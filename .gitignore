# Arquivos de Sistema
.DS_Store
*.swp
*~

# Arquivos gerados pelo Build
/build
/out
/target
**/build/
shared/version.txt

# Arquivos do IntelliJ
.idea/
*.iml
*.ipr
*.iws
.kotlin
# Arquivos do Gradle
.gradle/
gradle-app.setting
gradle/wrapper/gradle-wrapper.properties

# Arquivos locais
local.properties

# sl-cli rewrites gradle.properties at runtime; this is its restore point
gradle.properties.sl-cli.bak

# sl-cli node deps
tools/sl-cli/node_modules/
tools/sl-cli/package-lock.json

# demo/node-api locally installed deps
demo/node-api/node_modules/

# Claude Code — keep the shared preview launcher, ignore personal/local state
.claude/settings.local.json
.claude/worktrees/
# stray orphan lockfile (no root package.json)
/package-lock.json

# Talk preparation — personal decks, notes, critiques and QR assets.
# Kept out of git (the demos and library stay tracked; the prep does not).
docs/Slides.pen
docs/Architecture.pen
docs/SLIDES_PRESENTER_NOTES.md
docs/SLIDES_FOR_NOTEBOOKLM.md
docs/CRITIQUE.md
docs/DEBATE.md
docs/DEMO_RUNBOOK.md
docs/qr-code*.png